# PPCMtl2cr

## Scope and provenance

This contribution converts `PPCMtl2cr` in `src/dolphin/os/OSPPC.c` from an
asm-defined function to a natural-C wrapper. The privileged `mtspr 1017`
instruction remains...[truncated]